# "." is the engine package (Layers 0-4, storage — see README). "apps/*"
# holds the UI; Phase 2's Analog.js app lives at apps/web and depends on the
# engine package via the workspace protocol.
packages:
  - '.'
  - 'apps/*'
